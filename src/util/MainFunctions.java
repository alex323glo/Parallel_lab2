package util;

import static util.LabUtils.*;
import static util.TestUtils.generateMatrix;
import static util.TestUtils.generateVector;

public class MainFunctions {
    // TODO: f1: c = MAX(MA * MB) * (A * B)
    public static int f1(int size) {
        return 0;
    }

    // TODO: f2: MF = MAX(MG) * (MH * MK);
    public static int[][] f2(int size) {
        return null;
    }

    // TODO: f3: T = SORT(O + P) * TRANS(MR * MS);
    public static int[] f3(int size) {
        return null;
    }
}
